server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/javascript application/xml application/json image/svg+xml;

    # Root redirect to /app/
    location = / {
        return 301 /app/;
    }

    location = /app {
        return 301 /app/;
    }

    # Serve SPA routes under /app/
    location /app/ {
        try_files $uri $uri/ /app/index.html;
    }

    # Fallback for root
    location / {
        try_files $uri $uri/ /app/index.html;
    }

    # Cache static assets
    location ~* \.(?:css|js|jpg|jpeg|gif|png|ico|cur|gz|svg|svgz|mp4|ogg|ogv|webm|htc|woff|woff2|ttf|wasm|map)$ {
        expires 1y;
        access_log off;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }
}
